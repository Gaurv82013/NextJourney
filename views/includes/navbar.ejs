<link rel="stylesheet" href="/css/navbar.css">
<nav class="navbar navbar-expand-md bg-body-tertiary bottom-border fixed-top">
  <div class="container-fluid">
    <!-- Brand (logo + text) — stays visible on mobile, outside collapse -->
    <a class="navbar-brand d-flex align-items-center" id="nav-linkLogo" href="/listings">
      <img src="/img/NextJourney.png" alt="Logo" class="logo" />
      Explore
    </a>

    <!-- Toggler on the right -->
    <button class="navbar-toggler ms-auto" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Collapsible content: search + action links -->
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <!-- search bar -->
      <div class="ms-md-auto my-2 my-md-0 w-100">
        <form class="search-bar" method="GET" action="/listings">
          <input type="text" name="search" placeholder="Search your destination" />
          <button type="submit">
            <i class="fa-solid fa-magnifying-glass"></i>
          </button>
        </form>
      </div>

      <div class="navbar-nav ms-md-auto mt-2 mt-md-0">
        <div class="addLog">
            <a class="nav-link" href="/listings/new">Add new listing</a>
          
            <% if (typeof currentUser !== 'undefined' && currentUser) { %>
              <a class="nav-link" href="/logout">Logout</a>
            <% } else { %>
              <a class="nav-link" aria-current="page" href="/signup">Sign Up</a>
              <a class="nav-link" href="/login">Login</a>
            <% } %>
        </div>
      </div>
    </div>
  </div>
</nav>

